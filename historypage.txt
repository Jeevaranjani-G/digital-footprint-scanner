


Claim offer
You know what? Look at this picture very carefully. I will say everything one by one, okay? First thing, when I open the result page, as I already said, we are going to add one home icon to the three bars, I mean the sidebar, okay? And the result page will directly open in the homepage only. In the homepage, listen, in the attached image, that's a creative website without limits, right? I don't want something like that. When the first thing the user should see is, Hello and the username, okay? If the user is using sign-up or login, in both also, they will have username, right? So first, what they should see is, Hello, username. That should be the first line. It should be a little bit bigger, okay? Then below that, we have to, there should be wordings like, Welcome to our website, welcome to our website, Footprints, okay? Then the next line should be like that. Then below that only, maybe you can add a two-line of description, like, or something. Then below that, you know the features what all are our website can do. I mean, this analyzes pie chart giving the breaches, the features of our website, right? That should come with small boxes, like how there are small boxes in this, okay? And below that, it should have like a box, how is it? Here, there is Get Started, right? Instead of that, let us add something like, Start your digital footprints. Okay, let us also keep Get Started, okay? And then, if you look carefully, in the top of the image, in the center, there is something, right? To show support and all. Remove that, and in there, what I would like to add is, Get started by, okay, okay, no need, something like that, don't worry. Just remove that also, okay? Along with this, I want the three-line, you know, three-line icon in the left corner, okay? In the left side corner. So when I click that, you know, all the icons should show. Okay, well, in, I mean, when I click that, the sidebar should show, okay? Also add logger feature also in the sidebar as an extra icon. Do you get everything I say? If there is any doubt, clear it with me, and then let us write the result code, okay?

See, right, I have attached how my result page looks like, okay? But I'm not satisfied with it. The first thing is, the white color outline border, right? I don't want that. I want it totally removed. Then everything, I mean the letters, hello Jiva, welcome to footprints, everything is centered to the left side. I mean, sorry, it is moved to the left side, but I don't want it like that. I want it to be centered. Can you just use the finalized, right? It looks so awkward to look like this. I want it to give a very professional look, okay? So you know what you do? Make all the, make all this in the center, okay? Maybe increase the font size of hello Jiva, and below that, welcome to footprints and all is there, right? Yeah, just increase the font. Below that, arrange this, the description line neatly, and I also want that, you know, features box should be small and glowing, okay? It should be small and glowing, and below that, start your digital footprint is there, right? It should be totally centered below everything. Do you get what I say?

analyze this picture carefully these e the side bar icons that i have included in my web page okayy when i click each icon i want a page dedicated to it opening u understand ri8 soo lets start with the home page itself as this is not satisfactory u can see the hello jeeva ri8 where jeeva is the username i want it little more bigger centralized with a floating effect from the bg below that i want some little distance after which Welcome to Footprintz comes i want that also to have a floating and glowing effect and also give it a different colour then below that the features boxes are okay but let it have some distance between the welcome line and the start box then comes the start box where after i click it it leads me to the scan page. you got everything what i said ri8 i want only these changes so dont cahnge anything else ending up changing my lauyout alriri8. first let us finish the homepage


NOW we found the exact issue.

Your history page crashes AFTER saving because one of the saved items is missing some fields.

Most probably THIS line crashes:

item.platforms.map(...)
because some saved history objects do not contain platforms.

FIX THIS EXACTLY
Inside History.jsx

FIND:

{item.platforms.map((platform, i) => (
REPLACE WITH:

{item.platforms?.map((platform, i) => (
Then ALSO find:

<h1>{selectedScan.score}/100</h1>
Replace with:

<h1>{selectedScan.score || 0}/100</h1>
Find:

<h1>{selectedScan.breaches}</h1>
Replace with:

<h1>{selectedScan.breaches || 0}</h1>
Find:

<h1>{selectedScan.risk}</h1>
Replace with:

<h1>{selectedScan.risk || "Low"}</h1>
These fixes prevent blank-page crashes when some data is missing from saved history.


import { useState } from "react";

export default function History({ history, setHistory }) {

  if (!history) {
  return <h1 style={{ color: "white" }}>No History Found</h1>;
}

  const groupedHistory = {};
  const [selectedScan, setSelectedScan] = useState(null);

  /* ================= GROUP HISTORY ================= */

  history.forEach((item) => {

    const date = new Date(item.date);

    const monthYear = date.toLocaleString("default", {
      month: "long",
      year: "numeric",
    });

    if (!groupedHistory[monthYear]) {
      groupedHistory[monthYear] = [];
    }

    groupedHistory[monthYear].push(item);
  });

  /* ================= DETAIL VIEW ================= */

  if (selectedScan) {

    return (

      <div style={styles.modalOverlay}>

        <div style={styles.modalContent}>

          {/* CLOSE BUTTON */}
          <button
            style={styles.closeBtn}
            onClick={() => setSelectedScan(null)}
          >
            ✕
          </button>

          {/* TITLE */}
          <h1 style={styles.detailTitle}>
            🔍 Scan Report for @{selectedScan.username}
          </h1>

          {/* TOP GRID */}
          <div style={styles.detailGrid}>

            <div style={styles.detailCard}>
              <h3>🔒 Security Score</h3>
              <h1>{selectedScan.score || 0}/100</h1>
            </div>

            <div style={styles.detailCard}>
              <h3>⚠️ Risk Level</h3>
              <h1>{selectedScan.risk || "Low"}</h1>
            </div>

            <div style={styles.detailCard}>
              <h3>🚨 Breaches Found</h3>
              <h1>{selectedScan.breaches || 0}</h1>
            </div>

            <div style={styles.detailCard}>
              <h3>📈 Exposure Risk</h3>
              <h1>{selectedScan.exposure || 0}%</h1>
            </div>

          </div>

          {/* PIE CHART */}
          <div style={styles.bigCard}>

            <h2>📊 Exposure Analysis</h2>

            <div style={styles.fakeChart}></div>

            <div style={styles.chartLabels}>

              <div style={styles.labelRow}>
                <div style={styles.greenDot}></div>
                <span>Safe Exposure</span>
              </div>

              <div style={styles.labelRow}>
                <div style={styles.redDot}></div>
                <span>Risky Exposure</span>
              </div>

            </div>

          </div>

          {/* BAR CHART */}
          <div style={styles.bigCard}>

            <h2>📉 Platform Exposure</h2>

            <div style={styles.barContainer}>

              <div
                style={{
                  ...styles.bar,
                  width: "90%",
                }}
              >
                Instagram
              </div>

              <div
                style={{
                  ...styles.bar,
                  width: "75%",
                }}
              >
                GitHub
              </div>

              <div
                style={{
                  ...styles.bar,
                  width: "60%",
                }}
              >
                LinkedIn
              </div>

              <div
                style={{
                  ...styles.bar,
                  width: "40%",
                }}
              >
                Twitter
              </div>

            </div>

          </div>

          {/* PLATFORMS */}
          <div style={styles.bigCard}>

            <h2>🌐 Detected Platforms</h2>

            <div style={styles.platformRow}>

              {selectedScan.platforms?.map((platform, i) => (

                <span key={i} style={styles.platform}>
                  {platform}
                </span>

              ))}

            </div>

          </div>

          {/* SIMILAR USERS */}
          <div style={styles.bigCard}>

            <h2>👥 Similar Usernames Found</h2>

            <div style={styles.platformRow}>

              {selectedScan.similarUsers?.map((user, i) => (

                <span key={i} style={styles.platform}>
                  @{user}
                </span>

              ))}

            </div>

          </div>

          {/* RECOMMENDATION */}
          <div style={styles.bigCard}>

            <h2>💡 Recommendation</h2>

            <p style={styles.recommendation}>
              {selectedScan.recommendation ||
                "No recommendation available."}
            </p>

          </div>

        </div>

      </div>
    );
  }

  /* ================= MAIN HISTORY ================= */

  return (

    <div style={styles.container}>

      <h1 style={styles.title}>
        🕘 Scan History
      </h1>

      {history.length === 0 && (

        <div style={styles.emptyBox}>
          No scan history saved yet.
        </div>

      )}

      {Object.keys(groupedHistory).map((month) => (

        <div key={month} style={styles.monthSection}>

          <h2 style={styles.monthTitle}>
            {month}
          </h2>

          <div style={styles.cards}>

            {groupedHistory[month].map((item, index) => (

              <div
                key={index}
                style={styles.card}
                onClick={() => setSelectedScan(item)}
              >

                {/* DELETE */}
                <button
                  style={styles.deleteBtn}
                  onClick={(e) => {

                    e.stopPropagation();

                    const updatedHistory = history.filter(
                      (scan) => scan.id !== item.id
                    );

                    setHistory(updatedHistory);
                  }}
                >
                  🗑
                </button>

                {/* TOP */}
                <div style={styles.topRow}>

                  <h3 style={styles.username}>
                    @{item.username}
                  </h3>

                  <div
                    style={{
                      ...styles.riskBadge,

                      background:
                        item.risk === "High"
                          ? "#ef4444"
                          : item.risk === "Medium"
                          ? "#f59e0b"
                          : "#22c55e",
                    }}
                  >
                    {item.risk}
                  </div>

                </div>

                <p style={styles.info}>
                  📅 {item.date}
                </p>

                <p style={styles.info}>
                  ⏰ {item.time}
                </p>

                <p style={styles.info}>
                  🔒 Security Score: {item.score}/100
                </p>

                <div style={styles.platformRow}>
  {selectedScan.platforms?.map((platform, i) => (
    <span key={i} style={styles.platform}>
      {platform}
    </span>
  ))}
</div>

              </div>

            ))}

          </div>

        </div>

      ))}

    </div>
  );
}

/* ================= STYLES ================= */

const styles = {

  container: {
    width: "100%",
    minHeight: "100vh",
    padding: "40px",
    color: "white",
  },

  title: {
    fontSize: "42px",
    marginBottom: "35px",
    textAlign: "center",
    textShadow: "0 0 20px rgba(59,130,246,0.5)",
  },

  emptyBox: {
    background: "#1e293b",
    padding: "25px",
    borderRadius: "16px",
    textAlign: "center",
    opacity: 0.7,
  },

  monthSection: {
    marginBottom: "45px",
  },

  monthTitle: {
    fontSize: "28px",
    marginBottom: "20px",
    color: "#60a5fa",
  },

  cards: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit,minmax(320px,1fr))",
    gap: "20px",
  },

  card: {
    position: "relative",
    background: "rgba(30,41,59,0.95)",
    borderRadius: "18px",
    padding: "22px",
    boxShadow: "0 0 20px rgba(0,0,0,0.25)",
    cursor: "pointer",
    transition: "0.3s",
  },

  deleteBtn: {
    position: "absolute",
    top: "15px",
    right: "15px",
    background: "transparent",
    border: "none",
    color: "#ef4444",
    fontSize: "22px",
    fontWeight: "bold",
    cursor: "pointer",
  },

  topRow: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: "15px",
  },

  username: {
    fontSize: "24px",
  },

  
  info: {
    marginTop: "10px",
    opacity: 0.85,
  },

  platformRow: {
    display: "flex",
    flexWrap: "wrap",
    gap: "10px",
    marginTop: "18px",
  },

  platform: {
    background: "#334155",
    padding: "8px 14px",
    borderRadius: "999px",
    fontSize: "13px",
  },

  /* MODAL */

  modalOverlay: {
    position: "fixed",
    top: 0,
    left: 0,
    width: "100%",
    height: "100%",
    background: "rgba(0,0,0,0.88)",
    backdropFilter: "blur(10px)",
    overflowY: "auto",
    padding: "40px",
    zIndex: 5000,
  },

  modalContent: {
    maxWidth: "1200px",
    margin: "auto",
    color: "white",
  },

  closeBtn: {
    position: "fixed",
    top: "25px",
    right: "30px",
    width: "45px",
    height: "45px",
    borderRadius: "50%",
    border: "none",
    background: "#ef4444",
    color: "white",
    fontSize: "22px",
    cursor: "pointer",
    fontWeight: "bold",
  },

  detailTitle: {
    textAlign: "center",
    fontSize: "42px",
    marginBottom: "40px",
  },

  detailGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit,minmax(250px,1fr))",
    gap: "20px",
  },

  detailCard: {
    background: "#1e293b",
    padding: "25px",
    borderRadius: "20px",
    boxShadow: "0 0 20px rgba(59,130,246,0.25)",
  },

  bigCard: {
    marginTop: "30px",
    background: "#1e293b",
    padding: "30px",
    borderRadius: "20px",
  },

  fakeChart: {
    width: "230px",
    height: "230px",
    borderRadius: "50%",
    background:
      "conic-gradient(#22c55e 0% 60%, #ef4444 60% 100%)",
    margin: "30px auto",
    boxShadow: "0 0 30px rgba(59,130,246,0.4)",
  },

  chartLabels: {
    display: "flex",
    justifyContent: "center",
    gap: "30px",
    marginTop: "20px",
  },

  labelRow: {
    display: "flex",
    alignItems: "center",
    gap: "10px",
  },

  greenDot: {
    width: "15px",
    height: "15px",
    borderRadius: "50%",
    background: "#22c55e",
  },

  redDot: {
    width: "15px",
    height: "15px",
    borderRadius: "50%",
    background: "#ef4444",
  },

  barContainer: {
    marginTop: "30px",
    display: "flex",
    flexDirection: "column",
    gap: "18px",
  },

  bar: {
    background: "linear-gradient(90deg,#3b82f6,#9333ea)",
    padding: "14px",
    borderRadius: "12px",
    fontWeight: "bold",
    boxShadow: "0 0 20px rgba(59,130,246,0.3)",
  },

  recommendation: {
    marginTop: "15px",
    lineHeight: "1.7",
    opacity: 0.9,
    fontSize: "17px",
  },

};             this is my history.jsx u itself cheeck fully and give the crt code

I found the exact problem.
This part is crashing your History page:

{selectedScan.platforms?.map((platform, i) => (
INSIDE the MAIN history cards.

At that point, selectedScan is null, so the page crashes after saving.

You accidentally used selectedScan.platforms instead of item.platforms.

EXACT FIX
Find THIS near the bottom:

<div style={styles.platformRow}>
  {selectedScan.platforms?.map((platform, i) => (
    <span key={i} style={styles.platform}>
      {platform}
    </span>
  ))}
</div>
REPLACE IT WITH:

<div style={styles.platformRow}>
  {item.platforms?.map((platform, i) => (
    <span key={i} style={styles.platform}>
      {platform}
    </span>
  ))}
</div>
ONLY this change.

The issue is:

selectedScan ❌

item ✅

Because inside the history card loop, you must use item.

After this:

history page will load properly

saved scans will appear

clicking card will open detailed analysis

blank page issue will disappear completely.


noo it stillgoes blank

Then there is ONE more crash happening.

I checked your code carefully.

You are using this:

<div
  style={{
    ...styles.riskBadge,
BUT inside your styles object, riskBadge DOES NOT EXIST anymore.

So React crashes when it tries:

...styles.riskBadge
because it is undefined.

FIX
Inside your styles object, ADD THIS exactly BELOW username:

riskBadge: {
  padding: "8px 14px",
  borderRadius: "999px",
  fontSize: "13px",
  fontWeight: "bold",
  color: "white",
},
So it becomes:

username: {
  fontSize: "24px",
},

riskBadge: {
  padding: "8px 14px",
  borderRadius: "999px",
  fontSize: "13px",
  fontWeight: "bold",
  color: "white",
},

info: {
THAT is why the page still becomes blank after save.

After adding this:

Save file

Refresh page

Scan → Save → History

It should fully work now.


